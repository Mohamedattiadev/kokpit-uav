# Kokpit İHA — drone (Jetson) tarafı Python bağımlılıkları
#
# Jetson Orin Nano notu: OpenCV genellikle JetPack ile sistemde kuruludur ve
# CUDA hızlandırmalıdır. O durumda opencv satırını ATLA (sistem OpenCV'sini kullan).
# face_recognition, dlib derler — Jetson'da uzun sürebilir; swap aç.
#
# Versiyonlar bilerek == ile SABİTLENDİ (>= değil): JetPack/CUDA/OpenCV
# güncellemesinde sessiz uyumsuzluk (TensorRT engine, pymavlink dialect vb.)
# yarışma günü keşfedilmesin diye. Kasıtlı bir yükseltme gerekiyorsa, önce
# `KOKPIT_SIM=1 pytest tests/ -q` ile 369 testin hepsi yeşil kalmalı, sonra
# bu dosya güncellenmeli.

pymavlink==2.4.49
numpy==2.2.6
opencv-contrib-python==4.10.0.84  # cv2.aruco için (Jetson'da sistem OpenCV varsa atla)
pyserial==3.5                     # LoRa E32 UART (gerçek donanım)
face_recognition==1.3.0           # biyometrik (dlib gerektirir)
cryptography==49.0.0              # AES-128-CCM paket şifreleme (opsiyonel ama üretimde kullanılıyor)

# --- Geliştirme / test (uçuş için gerekmez) ---
pytest==9.1.1
pytest-timeout==2.4.0
flask==3.1.3                      # N3/N8 dashboard + canlı stream
matplotlib==3.10.9                # N3 replay grafikleri
requests==2.34.2                  # N6 hava durumu API
