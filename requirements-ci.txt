# CI bağımlılıkları (ubuntu-22.04 GitHub Actions)
# dlib derlemesi 5+ dk; face_recognition CI'da atlanır — OpenCV fallback yeterli.
#
# Versiyonlar == ile SABİTLENDİ — bkz. requirements.txt başındaki not.
pymavlink==2.4.49
numpy==2.2.6
opencv-contrib-python-headless==4.10.0.84
pyserial==3.5
cryptography==49.0.0          # AES-128-CCM için
pytest==9.1.1
pytest-timeout==2.4.0
ruff==0.15.20
flask==3.1.3
matplotlib==3.10.9
requests==2.34.2
